name: zahra_video_app
description: Text-to-video client with Unsplash + Google Maps backgrounds
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  dio: ^5.5.0
  url_launcher: ^6.3.0
  flutter_dotenv: ^5.1.0
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
